{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build"
  },
  "deploy": {
    "startCommand": "PORT=${PORT:-3000} && npx serve dist -s --listen tcp://0.0.0.0:$PORT"
  }
}
